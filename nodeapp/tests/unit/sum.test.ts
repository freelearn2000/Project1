
import sum from '../../src/sum'

describe('sample testing', () => {
    test('test1', () => {
        expect( sum(1, 2) ).toBe(3);
    });

    // test('test1', () => {
    //     expect( sum('a', 2) ).toBe(3);
    // });
});